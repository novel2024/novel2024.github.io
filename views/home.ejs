<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title || 'Novel Reader' %></title>
    <!-- Link to Bootstrap CSS -->
    <link rel="stylesheet" href="/bootstrap.min.css">
    <style>
        /* Custom styles for better readability on various screen sizes */
        body {
            padding-top: 20px;
            padding-bottom: 20px;
        }
        .container {
            max-width: 800px; /* Adjust as needed */
        }
        .story-list-item {
            margin-bottom: 10px;
            font-size: 1.1rem; /* Slightly larger font for story titles */
        }
        @media (max-width: 768px) {
            .story-list-item a {
                font-size: 1.2rem; /* Even larger on small screens */
            }
            h1 {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= locals.title || 'Available Stories' %></h1>
        <hr>

        <% if (locals.error) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } else if (locals.stories && stories.length > 0) { %>
            <ul class="list-group">
                <% stories.forEach(story => { %>
                    <li class="list-group-item story-list-item">
                        <a href="/stories/<%= story.folder_name %>"><%= story.title %></a>
                        <% if (story.description) { %>
                            <p class="text-muted small mt-1 mb-0"><%= story.description %></p>
                        <% } %>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p class="text-muted">No stories found at the moment. Please check back later.</p>
        <% } %>

        <hr>
        <footer class="text-center mt-4">
            <p class="text-muted small">&copy; Novel Reader App</p>
        </footer>
    </div>

    <!-- Optional: Bootstrap JS and Popper.js for interactive components if needed later -->
    <!-- <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script> -->
    <!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script> -->
</body>
</html>
